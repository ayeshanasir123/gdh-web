export interface ISingleFlightDetails {
  departureCityCode: string;
  departureTime: string;
  arrivalCityCode: string;
  arrivalTime: string;
  duration: string;
  stopCount: number;
  flightClass: string;
  flightNumber: string;
}
